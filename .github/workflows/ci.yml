name: DevOps Branch CI

on:
  push:
    branches:
      - main   # فقط روی main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI running only on MAIN branch"
